#assertion
CartCount = 0 
if CartCount!=2:
    pass # just used to bypass with the if condition 
# here he code expect the condition to b true else it will fail whenever assert gets a false result it break the code 
assert(CartCount==2)
assert(CartCount==0) # No error will come in this case 
 

